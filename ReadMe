# program
generate the Chinese recipe

Install the package first

pip install numpy

pip install tensorflow==2.1.0

pip install jieba

pip install re

pip install json

pip install matplotlib

pip install --upgrade gensim

pip install translate

pip install bs4

The python version is 3.6, and all python package codes must be installed to run normally.

The data web is https://maynoothuniversity-my.sharepoint.com/:u:/r/personal/deng_jie_2018_mumail_ie/Documents/mstx.rar?csf=1&web=1&e=cG2xkE

The code consists of 6 chapters in total:

Chapter 1 Data Crawler

Chapter 4 The initial deep network for recipe generation and fitting

Chapter 5 Improvement and Optimization of Deep Network

Chapter 6 100 random deep network tests
